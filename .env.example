# The local companion binds to loopback by default. Do not expose it publicly.
HKM_HOST=127.0.0.1
HKM_PORT=4317

# Development only: return deterministic graph-grounded output without calling a model.
HKM_HARNESS_MODE=live
