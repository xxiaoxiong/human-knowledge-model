{
  "name": "human-knowledge-model",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "description": "Human Knowledge Model website and local Codex-powered problem decomposition harness.",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "python scripts/build_site.py",
    "validate": "python scripts/validate_site.py",
    "start": "pnpm run build && node harness/server.mjs",
    "start:mock": "pnpm run build && node harness/server.mjs --mock",
    "lint:js": "node --check site/app.js && node --check site/harness.js && node --check harness/server.mjs && node --check harness/lib/graph.mjs && node --check harness/lib/codex-runner.mjs",
    "test": "node --test harness/tests/*.test.mjs",
    "check": "pnpm run lint:js && pnpm run test && pnpm run build && pnpm run validate"
  },
  "dependencies": {
    "@openai/codex-sdk": "0.153.4",
    "yaml": "2.9.0"
  },
  "engines": {
    "node": ">=18"
  }
}
